package com.example.geohousing.verification.domain;

/** Whether a decision took effect or was attempted against a case that does not exist. */
public enum VerificationDecisionOutcome {
  APPLIED,
  NOT_FOUND
}
