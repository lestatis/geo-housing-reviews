package com.example.geohousing.identity.domain;

/** The kind of identity-local data-subject request a user can make about their own account. */
public enum SelfServiceRequestType {
  EXPORT,
  DELETE
}
